jdk:
  - openjdk11
before_install:
  - sdk install java 11.0.10-open
  - sdk use java 11.0.10-open
install:
  - echo "Running a custom install command"
  - chmod +x gradlew # 添加执行权限
  - ./gradlew clean install -xtest
env:
  MYVAR: "custom environment variable"